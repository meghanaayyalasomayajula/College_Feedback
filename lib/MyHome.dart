import 'package:flutter/material.dart';
class MyHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Click on the cards'),
        backgroundColor: Colors.redAccent,
      ),
      body: Center(
        child: ListView(
          children: <Widget>[
          ],
        ),
      ),
    );
  }

}
